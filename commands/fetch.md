---
description: Save a source into raw/ without compiling it into the wiki
---

Use the {{skill}} skill and follow its **Fetch only** rules: save the source
into `raw/` exactly as the Ingest section's Fetch step says, log it, and stop.
Do not triage or compile.

Source: $ARGUMENTS

If no source was given, ask for one. Finish with the raw file path(s) you
wrote, and remind me that `/ingest` with no arguments compiles the backlog.
